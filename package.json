{
  "name": "radar",
  "version": "0.1.0",
  "private": true,
  "description": "RADAR — Réalta Adaptive Delivery Analysis and Routing. RTÉ delivery traffic intelligence and NS1 steering explainability (read-only v1).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:integration": "npm run test:integration --workspaces --if-present",
    "test:integration:postgres": "npm run test:integration:postgres --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
